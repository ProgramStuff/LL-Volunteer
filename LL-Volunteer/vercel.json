{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/static-build" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/src/" },
      { "src": "/(.*)", "dest": "/dist/$1" }
    ]
  }
  